{
  "name": "stats-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Daily traffic (Cloudflare Web Analytics) + search (Google Search Console) dashboard for David's domains",
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/render.js && node --check src/gsc.js && node --check src/config.js && node --check src/cloudflare.js && node --check src/bots.js && node --check src/opportunities.js && node --check src/signals.js && node --check src/urls.js && node --check scripts/refresh-stats.mjs && node --check scripts/rotate-refresh-key.mjs && node scripts/bots-check.mjs && node scripts/dashboard-check.mjs && node scripts/write-check.mjs && node scripts/render-check.mjs",
    "preview": "node scripts/render-check.mjs --write",
    "check:gsc": "node scripts/check-gsc-access.mjs",
    "rotate:refresh": "node scripts/rotate-refresh-key.mjs",
    "refresh": "node scripts/refresh-stats.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "dev": "wrangler dev",
    "release": "./deploy.sh",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "schema": "wrangler d1 execute stats-dashboard --remote --file=./schema.sql"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "wrangler": "^4"
  }
}
